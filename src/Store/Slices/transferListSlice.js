import { createSlice } from "@reduxjs/toolkit";




const transferListSlice = createSlice(({
    name : transferListSlice,
    initialState : {
        List : [],
        SelectedList : [],
        ChildList : []
    },
    reducers : {

    },
    extraReducers : {
    }
}))